<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Upload Resume</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/Admin_resume.css" />
  </head>
  <body>
    <a href="/admin/dashboard" class="position-fixed top-0 start-0 m-3">
      <img src="/img/arrow-left.png" alt="Back" style="width: 50px" />
    </a>
    <div class="upload-container">
      <h2><i class="bi bi-file-earmark-arrow-up"></i> Upload Resume</h2>
      <form method="POST" action="/admin/resume" enctype="multipart/form-data">
        <input
          type="text"
          class="form-control mb-3 custom-placeholder"
          name="title"
          placeholder="Title"
        />

        <input
          type="text"
          class="form-control mb-3 custom-placeholder"
          name="role"
          placeholder="Role"
        />
        <input
          type="text"
          class="form-control mb-3 custom-placeholder"
          name="quote"
          placeholder="Quote"
        />
        <input type="file" class="form-control mb-3" name="image" />
        <input
          type="file"
          class="form-control mb-3"
          name="resume"
          accept=""
        />
        <button class="btn btn-success" type="submit">Upload</button>
      </form>

      <% if (resume.url) { %>
      <p>
        Current Resume:
        <a href="<%= resume.url %>" target="_blank" download>
          Download
        </a>
      </p>
      <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
